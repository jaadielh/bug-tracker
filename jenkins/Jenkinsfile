pipeline {
    agent any

    stages {
        stage('Unit Tests Backend') {
            agent{
                docker {
                    image 'image/golang-junit:1.21'
                    reuseNode true
                }
            }
            steps {
                dir('bugtracker-backend') {
                    sh 'go test -v ./...'
                }
            }
        }
    }
}